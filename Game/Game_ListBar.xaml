<UserControl x:Class="Hosam_App.Game.GameIcon.Game_ListBar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Hosam_App.Game.GameIcon"
        mc:Ignorable="d" d:DesignHeight="110"
        d:DesignWidth="718">
    <Grid>
        <Border CornerRadius="55" Background="LightGray"
                Grid.ColumnSpan="5" Grid.RowSpan="2"
                Opacity="0.8" />
        <TextBlock Text="{Binding RelativeSource={RelativeSource FindAncestor, 
                               AncestorType={x:Type local:Game_ListBar}}, 
                               Path=GameName}"
                Foreground="Black"
                VerticalAlignment="Center"
                HorizontalAlignment="Center" FontSize="50" />
        <Button x:Name="ListButton" Width="120" Height="50"
                VerticalAlignment="Center"
                Margin="530,30,38,30" 
               
                Foreground="{x:Null}" Background="#FF0074FF"
                BorderBrush="{x:Null}">

            <TextBlock Text="Install" FontSize="30" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" />
        </Button>

    </Grid>
</UserControl>
